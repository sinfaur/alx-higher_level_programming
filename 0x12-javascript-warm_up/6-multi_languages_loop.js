#!/usr/bin/node
// a script that prints three srings to the console using a loop

const arr = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}
